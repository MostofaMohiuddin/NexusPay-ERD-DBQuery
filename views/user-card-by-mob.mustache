<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Get User By ID</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <h1>Get User's Card</h1>
    
    <div class="code-container">
      <h2>
        <p>SELECT * FROM cards</p>
        <p>WHERE cards.user_id = </p> 
        <p>(SELECT user_id FROM users WHERE mobile_no = input.mobile_no AND pin = input.pin)</p>
      </h2>
    </div>

    <br />
    <div class="row">
      <form method="POST" action="/user/card">
        <span>
          <input
            class="swing"
            id="mobileNo"
            type="text"
            placeholder="Mobile No"
            name="mobileNo"
          /><label for="mobileNo">Mobile</label>
        </span>
        <span>
          <input
            class="swing"
            id="pin"
            type="password"
            placeholder="Pin"
            name="pin"
          /><label for="pin">Pin</label>
        </span>
        <button type="submit" class="btn-gradient cyan">Submit</button>
      </form>
    </div>

    <div class="tbl-header">
      <table cellpadding="0" cellspacing="0" border="0">
        <thead>
          <tr>
            <td><strong>card_no</strong></td>
            <td><strong>card_holder_name</strong></td>
            <td><strong>card_expiry_date</strong></td>
            <td><strong>user_id</strong></td>
            <td><strong>card_type</strong></td>
          </tr>
        </thead>
      </table>
    </div>
    <div class="tbl-content">
      <table cellpadding="0" cellspacing="0" border="0">
        <tbody>
          {{#cardInfo}}
          <tr>
            <td>{{ card_no }}</td>
            <td>{{ card_holder_name }}</td>
            <td>{{ card_expiry_date }}</td>
            <td>{{ user_id }}</td>
            <td>{{ card_type }}</td>
          </tr>
          {{/cardInfo}}
        </tbody>
      </table>
    </div>

    <br />
    <br />
    <div class="query-page-btn">
      <form method="GET" action="/query">
        <button type="submit" class="btn-gradient cyan">
          Main Query List page
        </button>
      </form>
    </div>
    <br />
    <br />

    <script>
      $(window)
        .on("load resize ", function() {
          var scrollWidth =
            $(".tbl-content").width() - $(".tbl-content table").width();
          $(".tbl-header").css({ "padding-right": scrollWidth });
        })
        .resize();
    </script>
  </body>
</html>
